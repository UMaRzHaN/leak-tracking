# Оставшаяся работа

**Дата:** 2026-08-20
**Состояние на:** `cfc7b6a` (ветка `claude/component-registry`)

Всё, что ниже, проверено на текущем коде: цифры сняты прогонами, а не взяты из
прошлых отчётов. Прошлая редакция этого файла описывала ветку
`claude/import-worker` и с тех пор частью закрылась — что именно, сказано в §7.

Состояние гейтов сейчас:

| Гейт                                       | Состояние                      |
| ------------------------------------------ | ------------------------------ |
| `lint`, `format:check`, `typecheck`        | зелёные                        |
| `vitest run` — 2062 теста, 222 файла       | зелёный                        |
| `build`, `check:bundle`                    | зелёные (с предупреждениями)   |
| `test:coverage` + `check:coverage-ratchet` | **красный — см. §1**           |
| `verify:release`                           | **не проходит** из-за покрытия |

---

## 1. Покрытие: гейт красный — это regression текущей ветки

`npm run test:coverage` падает на десяти порогах. Все они в файлах, которые эта
ветка трогала: пороги ставились «на два пункта ниже измеренного», а новый код
добавил ветвления без тестов на них.

| Файл                                          | Что не дотягивает                                  |
| --------------------------------------------- | -------------------------------------------------- |
| `src/pages/MapPage/MapPage.jsx`               | lines 92.3/95, functions 88.9/95, branches 62.5/75 |
| `src/pages/Settings/Settings.jsx`             | lines 65.4/68, functions 50/51                     |
| `src/pages/Settings/hooks/useSettingsPage.js` | lines 89.2/94, statements 86.2/92                  |
| `src/repositories/nativeLeakStorage.js`       | branches 61.2/62                                   |

Что за ветвления:

- **MapPage** — переключатель баз и всё, что от него зависит (какие кнопки
  показывать, что выгружать в KML). Тесты на это есть у `MapControls` и
  `componentMarkers`, но не на самой странице.
- **Settings** — маршрутизация импорта (`handleImportFile`) и вливание
  инвентаризации: `handleImportInventory` целиком без теста.
- **nativeLeakStorage** — из него уехал `isPluginUnavailableError` в общий
  `nativeSqlitePlugin.js`, и счёт веток в файле изменился. Порог опустить
  нельзя (политика: только вверх), значит нужен тест на ветку отката.

Пороги опускать нельзя — `scripts/coverage-policy.json` разрешает им только
расти. Это правильно; чинить надо тестами.

**Заодно:** у всего реестра нет ни одного файлового порога, хотя измеренное
покрытие у него высокое и он лежит ровно на том же критическом пути (данные,
которые нельзя потерять), что и остальное в списке:

```
domain/componentHistory.js            100 / 92.85
domain/componentMerge.js              100 / 89.28
domain/componentRegistry.js           100 / 89.09
services/inventory/inventoryArchive.js       100 / 91.30
services/inventory/inventorySheet.js         100 / 84.37
services/inventory/inventoryBackupSheet.js  98.24 / 85.29
services/inventory/inventoryImport.js       94.91 / 81.81
services/backup/componentArchive.js         97.14 / 75.00
services/backup/componentPhotoArchive.js    90.38 / 77.14
repositories/nativeComponentStorage.js      96.22 / 82.22
services/import/importRouting.js            90.32 / 81.25
```

(lines / branches, по свежему `coverage-summary.json`.)

Пороги для них надо выставить сейчас, пока цифры высокие: потом они сползут, и
никто не заметит.

---

## 2. Бюджет сборки — 3,5 КБ до падения

```
initialRaw    405 284 / 419 840   (96,5 %)
initialGzip   124 479 / 128 000   (97,2 %)
appGraphJs  2 436 086 / 2 560 000 (95,2 %)
excelWorker 1 141 135 / 1 179 648 (96,7 %)
```

`npm run check:bundle` падает, а не предупреждает. Запаса в стартовом чанке —
около 3,5 КБ сырых. Практические следствия:

- Любая новая **статическая** зависимость в стартовом графе уронит CI. Всё
  тяжёлое грузится через `await import` и только на своём экране: словари
  оборудования, `exceljs`, `jszip`, локали, карта.
- Реестр это уже соблюдает: блок `config.components` ленивый, шапка читает
  карточки динамическим импортом (`useRegistryLocationSource`), карта — тоже
  (`useMapComponents`).
- Если запас понадобится, первый кандидат — вынести из стартового графа то, что
  нужно не каждому запуску. Мерить `npm run build:analyze`.

---

## 3. Данные, которые придётся мигрировать

### Карточки, заведённые до слияния полей

Ключ наименования был `component_name`, стал `component`. Такие карточки
показываются как «Без наименования». Тестовые проще перезавести; если к моменту
выпуска появятся реальные — нужна миграция при чтении (`normalizeComponent`
подходящее место: он и так проходит по каждой карточке).

### Словарь наименований — причесать до начала обхода

`src/data/leak/fieldDictionary.js`: опечатки («Маноментр», «Дросель»,
«Уровномер», «Газораспределетельный пункт») и расхождения с реестром («Клапан
обратный» против «Обратный Клапан»). Голосовой ввод и автодополнение сводят
сказанное именно к этим строкам, поэтому опечатка закрепится в данных.

Делать **до** первого настоящего обхода: после — придётся мигрировать уже
заполненные карточки.

---

## 4. Реестр компонентов: что осталось

- **Связь утечки с карточкой компонента.** Ключи (`component`,
  `actuator_type`, `connection_type`, `installation_type`, `lat`, `lng`,
  `photo`) намеренно общие, чтобы это стало прямым копированием, а не таблицей
  соответствий. Сама связь не сделана.
- **Тип проекта для архива инвентаризации выводится.** Архив не несёт ни имени,
  ни типа; тип берётся как единственный, у которого объявлен реестр
  (`componentRegistryProjectTypes()`). Появится второй такой тип — вывод
  перестанет быть однозначным, и первый экран попросит выбрать. Проверка есть,
  поведение описано, но UI выбора типа именно для этого случая не рисовался.
- **Реестр только у Upstream.** Для midstream и downstream блок
  `config.components` не объявлен — вкладки нет. Понадобится — нужны свои
  словари оборудования и колонки выгрузки.
- **Нет e2e.** В `e2e/` нет ни одного сценария с реестром: ни заведения
  карточки, ни выгрузки архива, ни импорта его обратно. Скрипт скриншотов
  (`scripts/capture-manual-screenshots.mjs`) проходит этот путь целиком и может
  служить каркасом.
- **Нет instrumented-теста на новый ключ SQLite.** `nativeComponentStorage`
  пишет в ту же базу, что и утечки, под ключом `components:<папка>`.
  `LeakDatabaseStoreInstrumentedTest` про это ничего не знает — проверить, что
  две базы в одном файле не мешают друг другу, стоит на устройстве.

---

## 5. Готовность к выпуску

### Ветка

`claude/component-registry` отведена от `claude/import-worker`, а та на 527
коммитов впереди `main`. Полный релизный путь через CI ни разу не проходили:
семь job'ов, включая эмулятор API 24/35 и WebKit, локально не воспроизводятся.
Прежде чем выпускать — довести до `main` и увидеть их зелёными.

### Android

Подпись релиза настроена и проверена (`npm run android:release`, отпечаток
сверен строкой). Instrumented-тесты прогонялись на эмуляторе API 37 — 15 тестов,
0 падений. **API 24 (`minSdkVersion`) проверен только в CI**: локально нет
`cmdline-tools`, а значит и образов 24/35.

### Веб

Требование к развёртыванию — в README, раздел «Заголовки, которые обязан
отдавать сервер». CSP живёт в `<meta>`, а `frame-ancestors`, `report-uri` и
`sandbox` в meta-теге игнорируются по спецификации: пока сервер не отдаёт
`Content-Security-Policy: frame-ancestors 'none'`, веб-версию можно встроить в
чужой iframe. Там же HSTS, `X-Content-Type-Options`, `Referrer-Policy` и
`Permissions-Policy`.

---

## 6. Документация

- **Android-PDF устарел.** `docs/manual/android/manual.html` (и собранный из
  него `docs/Leak-Tracker-Android-Manual.pdf`) не знает про реестр и описывает
  две кнопки импорта, которых больше нет. Обновление требует снимков с живого
  телефона: `node scripts/capture-android-manual-screenshots.mjs`, затем
  `node scripts/build-android-manual-pdf.mjs`.
- **`HANDOFF_INVENTORY.md`** — объяснение к непринятой ветке. После влития его
  ценность сводится к двум разделам («почему так решено» и «известные дыры»);
  их логично перенести в `docs/`, остальное удалить.
- `README.md` и `docs/MANUAL.md` приведены в соответствие с кодом 2026-08-20
  (реестр, схемы, один импорт, два архива, раскладка папок).

---

## 7. Что закрылось с прошлой редакции

Чтобы не искали заново:

- **Миграция i18n** — закончена, список долга в `locales.test.js` пуст.
- **Покрытие `useLeakDetailsPersistence` и `offlineMap`** — тесты появились.
- **Подпись Android-релиза и первый instrumented-прогон** — сделаны.
- **Мажоры** — подняты все, кроме сборочной цепочки (см. ниже).
- **Производительность импорта/экспорта** — разбор уехал в воркер, двойная
  распаковка убрана.

---

## 8. Решения, к которым не надо возвращаться

Это не задачи. Здесь записано, почему сделано так, чтобы это не переспорили
заново, не зная причины.

- **CRC32 при экспорте не ускоряем.** Slice-by-8 даёт 41 → 32 мс на 16 МБ
  (1,28×) ценой восьми таблиц и развёрнутого цикла в функции, от которой
  зависит целостность архива. Если появятся жалобы именно на скорость
  экспорта — считать CRC в воркере, а не ускорять цикл.
- **Vite 6 → 8 не поднимаем.** В восьмой сборка переехала на rolldown, который
  не принимает объектную форму `manualChunks` — только функцию. Значит меняется
  разбиение чанков, а вместе с ним и все бюджеты. Отдельная работа, не бамп.
- **Отчёт по утечкам не везёт инвентаризацию.** Это два разных архива для двух
  разных получателей. ZIP Backup по-прежнему везёт всё — он про перенос
  проекта, а не про отчёт.
- **Реестр делит SQLite с утечками.** Таблица плагина держит записи по ключу
  проекта и `id` и внутрь полезной нагрузки не смотрит; отсюда транзакционность
  и невозможность столкновения. Отдельная база не нужна.
- **Уникальность номеров компонентов не обеспечивается.** Диапазоны не
  раздаются (решение заказчика), чужие номера не видны. Дубль предупреждает, но
  не блокирует; совпадение с другого устройства разбирается при сведении.

---

## Порядок, который я бы предложил

1. **Покрытие** (§1) — гейт красный, и это блокирует `verify:release`. Сначала
   тесты на новые ветки, следом пороги для реестра, пока цифры высокие.
2. **Словарь наименований** (§3) — до первого настоящего обхода, иначе опечатки
   закрепятся в данных.
3. **Влить в `main`** (§5) и увидеть CI целиком — единственный способ проверить
   API 24 и WebKit.
4. **e2e на реестр** (§4) — каркас уже есть в скрипте скриншотов.
5. **Android-PDF** (§6) — когда будет устройство под рукой.
6. Связь утечки с компонентом (§4) — самостоятельная фича, не блокер.
