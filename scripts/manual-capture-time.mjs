/**
 * Момент, в котором снято всё руководство.
 *
 * Снимки полагалось бы переснимать и сравнивать, но сравнивать было нечего:
 * сид опирался на `Math.random` и `Date.now`, и каждая пересъёмка меняла все
 * кадры разом — другие утечки, другие даты. Отличить устаревший экран от
 * просто переставшегося было нельзя, и снимки не проверялись вовсе.
 *
 * Поэтому момент один и записан здесь: сид получает его как `SEED_NOW`, а часы
 * страницы замирают на нём же. Данные от этого повторяемы, и настоящая правка
 * интерфейса видна в диффе как правка, а не тонет в перетасовке.
 *
 * Замирает `Date.now`, а не приложение: таймеры идут своим ходом.
 */
export const MANUAL_CAPTURE_TIME = new Date(
  process.env.MANUAL_CAPTURE_TIME ?? "2026-08-20T09:00:00.000Z",
);
