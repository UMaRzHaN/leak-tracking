{
  // Строгая проверка null/undefined — храповик по каталогам, а не по всему src.
  //
  // Под strictNullChecks весь проект даёт около восьмисот ошибок, и включать
  // её разом означало бы не включить никогда. Но распределены они крайне
  // неравномерно: на pages/ и features/ приходится больше половины, а на
  // domain/ и utils/ — где живут расчёт выбросов, разбор архивов и жизненный
  // цикл записи — их было двадцать шесть.
  //
  // Список include может только расти. repositories/ добавлен: 56 ошибок на
  // замере, и разбор каждой оказался не формальностью — нашлись обращения к
  // соединению IndexedDB, которое между проверкой и колбэком успевает
  // обнулиться, и проверка результата разбора, у которой поле замечаний было
  // то ли есть, то ли нет.
  //
  // components/ добавлен: 34 ошибки на замере вместо ожидавшихся 22 — включение
  // каталога тянет за собой и то, на что он опирается, так что под проверку
  // попали заодно app/project/ и hooks/. Половину дал один createContext(null):
  // из-за него useProjectData() возвращал null, и в шапке любое обращение к
  // проекту становилось обращением к never.
  //
  // hooks/ и services/ добавлены. В services/ разбор ошибок вывел на настоящую:
  // запись состояния синхронизации выходила через пустой `return`, когда
  // проекта нет, а всё остальное её тело возвращает промис — и откат импорта
  // вешает на результат `.catch`. Вместо причины сбоя читатель получил бы
  // падение самого отката.
  //
  // app/ каталогом не добавляется: App.jsx тянет маршруты, маршруты — все
  // страницы, страницы — всё остальное, и включение даёт 581 ошибку на весь
  // проект при 18 в самом app/. Он идёт последним, а не следующим.
  //
  // Оставшиеся ступени по цене входа: features/ (173), pages/ (543 — считая
  // всё, что они тянут), и уже за ними app/.
  "extends": "./tsconfig.check.json",
  "compilerOptions": {
    "strictNullChecks": true
  },
  // types/ обязателен: там объявлены Error.code и расширения window, без
  // которых замер показывает восемь несуществующих ошибок.
  "include": [
    "src/types/**/*",
    "src/domain/**/*.js",
    "src/utils/**/*.js",
    "src/repositories/**/*.js",
    "src/components/**/*.js",
    "src/components/**/*.jsx",
    "src/hooks/**/*.js",
    "src/services/**/*.js"
  ],
  "exclude": ["src/**/*.test.js", "src/**/*.test.jsx"]
}
