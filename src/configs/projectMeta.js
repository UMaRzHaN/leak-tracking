/**
 * Название, подпись и папка для каждого типа проекта.
 *
 * Отдельным модулем от `projects.js`, хотя лежало там же. Эти двадцать строк
 * читают шапка, список проектов, миграция и хранилище — всё, что работает с
 * первого экрана. А рядом в том же модуле стоят три полных конфига типа: поля,
 * словари, шаги формы, колонки выгрузки. Импорт лёгкой константы затаскивал
 * тяжёлого соседа целиком — тридцать килобайт в начальный чанк ради заголовка
 * «Upstream».
 */
export const PROJECT_META = {
  upstream: {
    title: "Upstream",
    description: "Добыча",
    folder: "upstream",
  },
  midstream: {
    title: "Midstream",
    description: "Транспортировка и хранение",
    folder: "midstream",
  },
  downstream: {
    title: "Downstream",
    description: "Переработка и сбыт",
    folder: "downstream",
  },
};
