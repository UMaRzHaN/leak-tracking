/**
 * Какую из двух баз проекта показывает карта.
 *
 * Лежит отдельным модулем, а не рядом с `useMapPage`: значение читает шапка
 * приложения, чтобы переключить с ней и выбор места, а `useMapPage` тянет за
 * собой карту целиком. Общий вход означал бы карту в стартовом графе, где
 * бюджет и без неё занят на девять десятых.
 */
export const MAP_BASE = { LEAKS: "leaks", COMPONENTS: "components" };
