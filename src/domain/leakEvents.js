/**
 * Лента событий утечки — общий вход.
 *
 * Модуль разделён на три по разным вопросам: сама модель события
 * (`leakEventsCore`), разворачивание старых записей в ленту при чтении
 * (`leakEventsMigration`) и вопросы о починке, на которые лента отвечает
 * вместо прежних вех (`leakEventsRepairs`). Вход остался один: лента
 * спрашивается из полусотни мест, и разводить их по трём адресам значило бы
 * заставить каждого читателя знать, какая часть где живёт.
 */
export * from "./leakEventsCore";
export * from "./leakEventsMigration";
export * from "./leakEventsRepairs";
