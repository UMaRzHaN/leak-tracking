import "@testing-library/jest-dom/vitest";
import { configure } from "@testing-library/dom";

/*
 * Секунды ожидания по умолчанию не хватает не приложению, а машине.
 *
 * `SchemaList > closes the drawing with Escape` падал примерно раз на десять
 * полных прогонов и никогда — в одиночку: диалог закрывается сразу, но
 * воркеру, делящему десять ядер с тремя сотнями файлов, случается не успеть
 * отдать управление за отведённую секунду. Версию посерьёзнее — застрявшую
 * запись в общем стеке модалок, из-за которой Escape слушала бы не та
 * модалка, — проверили отдельно и не подтвердили: стек чистится и за десять
 * открытий подряд, и при размонтировании.
 *
 * Три секунды настоящую поломку не прячут: закрытия, которого нет, не будет и
 * через три секунды, а сообщение об отказе `waitFor` понятнее, чем обрыв по
 * общему сроку теста в пять секунд, в который такое ожидание упёрлось бы
 * следом.
 */
configure({ asyncUtilTimeout: 3000 });
