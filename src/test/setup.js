import "@testing-library/jest-dom/vitest";
import { configure } from "@testing-library/dom";

/*
 * Секунды ожидания по умолчанию не хватает не приложению, а машине.
 *
 * `SchemaList > closes the drawing with Escape` падал примерно раз на десять
 * полных прогонов и никогда — в одиночку. Причину искали, а не угадывали:
 *
 *   - версию про застрявшую запись в общем стеке модалок, из-за которой
 *     Escape слушала бы не та модалка, проверили отдельной пробой и
 *     опровергли: стек чистится и за десять открытий подряд, и при
 *     размонтировании;
 *   - воспроизвели: полный прогон под тремя лишними процессами, занимающими
 *     процессор, роняет тест на втором заходе, ровно на 1019 мс — то есть в
 *     секундный предел ожидания, а не по какой-то своей причине;
 *   - проверили, что дело не в потерянном нажатии: с тремя секундами под той
 *     же нагрузкой набор прошёл двенадцать раз из двенадцати. Потеряйся
 *     нажатие — не помогли бы и три секунды.
 *
 * Значит, диалог закрывается как надо, просто воркеру, делящему десять ядер с
 * тремя сотнями файлов, случается не получить процессор за отведённую секунду.
 *
 * Три секунды настоящую поломку не прячут: закрытия, которого нет, не будет и
 * через три секунды. Отдельным тестом проверено, что отказ приходит через
 * 3.0 с как понятный отказ `waitFor`, а не как обрыв по общему сроку теста в
 * пять секунд, в который такое ожидание упёрлось бы следом.
 */
configure({ asyncUtilTimeout: 3000 });
