import { registerPlugin } from "@capacitor/core";

/**
 * Нативная половина локальной синхронизации.
 *
 * Отдельным модулем, потому что обращаются к ней двое: сам обмен и канал,
 * которым архив уходит в нативную часть. Регистрировать плагин дважды — значит
 * заводить два объекта там, где нужен один.
 */
export const LocalSync = registerPlugin("LocalSync");
