/**
 * Имена внутри архива инвентаризации.
 *
 * Отдельным модулем без единого импорта, потому что нужны они по обе стороны
 * границы: лист называет сборщик книги, который живёт в воркере, а папки —
 * сборщик архива, который остаётся на главном потоке. Общий модуль потяжелее
 * утащил бы за собой ExcelJS туда, где его быть не должно.
 */
export const INVENTORY_SHEET_NAME = "Inventorization";
export const INVENTORY_PHOTO_DIR = "Photos";
export const INVENTORY_SCHEMA_DIR = "Schemes";
